<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
<item android:id="@+id/action_lastpage"
        android:orderInCategory="400"
        app:showAsAction="always"
        android:icon="@drawable/right_arrow"
        android:title="Next"/>
    <item android:id="@+id/action_back"
        android:orderInCategory="300"
        app:showAsAction="always"
        android:icon="@drawable/left_arrow"
        android:title="Back"/>
    <item android:id="@+id/zoominout"
        android:orderInCategory="200"
        app:showAsAction="always"
        android:icon="@drawable/zoom_in"
        android:title="ZoomInOut"/>

    <item android:id="@+id/search"
            android:title="search Pages"
        android:icon="@drawable/ic_search_24dp"
        app:showAsAction="always"/>

</menu>